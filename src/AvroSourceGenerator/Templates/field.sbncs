{{~ include 'comment' documentation: $.field.Documentation ~}}
{{~ include 'aliases' schema: $.field ~}}
public {{~ UseRequiredProperties && !$.field.Default ? ' required ' : ' ' ~}} {{ $.field.Type }} {{ $.field.Name }} { get; {{ UseInitOnlyProperties ? 'init' : 'set' }}; }
{{-
if $.field.Default
    $" = {$.field.Default};"
else if !$.field.Type.IsNullable && UseNullableReferenceTypes && !UseRequiredProperties
    ' = default!;'
end }}

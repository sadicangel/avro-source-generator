{{~ $.override ? 'public override void Put' : 'void global::Avro.Specific.ISpecificRecord.Put' ~}}(int fieldPos, {{ ObjectType }} fieldValue)
{
    switch (fieldPos)
    {
        {{~ for field in $.schema.Fields ~}}
        case {{ for.index }}:
            Set_{{ field.Name.SchemaName }}(this, {{ include 'apache.put_field_value' field: field }}); break;
            [global::System.Runtime.CompilerServices.UnsafeAccessor(global::System.Runtime.CompilerServices.UnsafeAccessorKind.Method, Name = {{ $"\"set_{ field.Name.SchemaName }\"" }})]
            extern static void Set_{{ field.Name.SchemaName }}({{ $.schema.CSharpName }} obj, {{ field.Type }} value);
        {{~ end ~}}
        default:
            throw new global::Avro.AvroRuntimeException($"Bad index {fieldPos} in Put()");
    }
}
